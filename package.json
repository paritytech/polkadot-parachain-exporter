{
  "name": "polkadot-parachain-exporter",
  "description": "Prometheus exporter for monitoring Polkadot Relay- and Parachain liveness",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "build": "./node_modules/.bin/rimraf ./build && ./node_modules/.bin/tsc",
    "start": "node --unhandled-rejections=strict build/index.js"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "@polkadot/api": "^6.3.1",
    "bn.js": "^5.2.0",
    "dotenv": "^10.0.0",
    "prom-client": "^14.0.0"
  }
}
